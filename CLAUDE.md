# CLAUDE.md — 이 저장소에서 작업하는 규칙

Claude Code 등 코딩 에이전트가 이 저장소를 고칠 때 따라야 하는 지침입니다.
사람이 읽어도 됩니다. **아래 "깨면 안 되는 규칙"은 실제 사고를 겪고 만든 것들입니다.**

---

## 1. 이 저장소는 무엇인가

기부 약정 플랫폼입니다. 두 웹이 API로만 연결됩니다.

```text
qsight_corp/      기관용 — 모금 담당자
  backend/          FastAPI. 사업·서식·약정·전자서명. 기관용 화면도 여기서 서빙  :8080
  frontend/         기관용 화면 (W1~W10, 바닐라 JS)
qsight_client/    기부자용 — 기부자
  server/           Node/Express. AI 챗봇, 기관 API 중계                        :4000
  client/           React(Vite)                                                :5173
```

실행 순서는 기관용 → 기부자용 서버 → 기부자용 화면입니다.
자세한 명령은 [README.md](README.md#qsight-실행하기)에 있습니다.

```bash
python -m uvicorn server.main:app --port 8080 --app-dir qsight_corp/backend --reload
npm --prefix qsight_client/server run dev
npm --prefix qsight_client/client run dev
```

**키가 하나도 없어도 대부분 동작합니다.** 모두싸인 자격증명이 없으면 기관용이 데모 모드로
전환됩니다. 작업 후 확인할 때 키를 요구하지 마세요.

---

## 2. 깨면 안 되는 규칙

### 2-1. LLM에게 맡기는 일과 맡기지 않는 일

이 프로젝트의 핵심 설계입니다. **바꾸지 마세요.**

| 일 | 담당 |
| --- | --- |
| 사용자 말에서 값 추출 | **LLM** (tool call, `temperature: 0`) |
| 무엇이 아직 비었는지 판단 | 코드 (`fields.js`의 스키마-값 차집합) |
| 사용자에게 보여줄 문장 | 코드 (`format.js`가 label·type·options로 조립) |
| 유언 대본 문장 | 코드 (`legacyScript.js`가 템플릿에 값을 끼움) |

챗봇이 내보내는 문장을 LLM이 쓰지 않기 때문에 **스키마에 없는 질문이나 지어낸 설명이
화면에 나갈 수 없습니다.** 개발 중 실제로 "2007년생"을 미성년으로 판단해 보호자 연락처를
요구하거나 법적 근거를 지어내는 문제가 있었고, 이 구조로 해결했습니다.

**"LLM에게 문장을 만들게 하면 코드가 줄어든다"는 제안을 하지 마세요.** 그게 문제였습니다.

### 2-2. 유언 대본은 코드가 조립한다

`qsight_client/server/config/legacy-spec.json`의 템플릿에 값을 끼워 넣습니다.
"오늘은 ○년 ○월 ○일입니다" 한 줄이 빠지면 **유언 전체가 무효**입니다.
법정 요건 문장을 확률적 생성에 맡길 수 없습니다.

`legacySpec.js`가 서버 기동 시 검증하고, 실패하면 **`process.exit(1)`으로 서버를 멈춥니다.**
이 검증을 "너무 엄격하다"며 경고로 낮추지 마세요. 조용히 넘어가면 안 되는 종류의 실패입니다.

- 법정 요건 5종 태그(`donor_name` `date` `content` `witness_confirm` `witness_name`)가 대본에 다 있는가
- `review_checklist`가 그 5종과 1:1로 대응하는가
- 대본 플레이스홀더가 전부 정의된 값인가

### 2-3. 법률에 기대는 문구는 JSON 한 파일에만

대본 문안, 챗봇 질문 항목, 재산 특정 방식, 증인 결격 문항, 자가 확인 항목, 화면 고지 문구는
**전부 `legacy-spec.json`에만** 둡니다. 화면(JSX)에 문구를 하드코딩하지 마세요.

법률 검토 결과가 나오면 **JSON만 고치고 서버를 재시작**하면 반영됩니다. 프런트 재빌드도
코드 수정도 필요 없어야 합니다.

`spec.forbidden_phrases`에 적힌 표현은 어디에도 쓰지 마세요.

```
"유효한 유언이 완성되었습니다"  "법적 효력이 발생했습니다"  "약정이 체결되었습니다"
```

효력은 법원이 판단합니다. 화면은 그렇게 말하지 않습니다.

### 2-4. 녹음 화면에서 하면 안 되는 것

- **파일 업로드 input을 만들지 않습니다** — 재생본·편집본이 들어오면 무효 사유입니다
- **일시정지 버튼을 두지 않습니다** — 증인 진술이 따로 녹음되면 무효로 본 판례가 있습니다
- 중단한 녹음은 업로드하지 않고 버립니다
- **녹음 중에는 음성 안내(TTS)를 잠급니다** — 합성음이 마이크에 섞이면 본인 아닌 목소리가
  녹음유언에 들어갑니다. `SpeakButton`에 `disabled`를 넘겨 막습니다

### 2-5. 기관에 유언 내용을 보내지 않는다

녹음 파일, 대본 전문, 증인 이름·연락처는 **기부자용 서버에만** 둡니다.
기관용에는 "녹음을 마쳤다는 사실"(시각·길이·해시·증인 유무·자가확인 수)만 보냅니다.

생전에 수증 기관이 유언 내용을 열람하면 부당한 영향력 행사 의혹의 빌미가 됩니다.
편의를 이유로 이 경계를 넘지 마세요.

### 2-6. 질문 목록을 코드에 박지 않는다

챗봇이 무엇을 물어볼지는 **기관이 등록한 계약서 서식**에서 옵니다. 기관이 서식을 바꾸면
질문이 저절로 따라 바뀝니다. 특정 항목명(`amount`, `frequency` 등)을 조건문에 넣지 마세요.

유산기부는 여기에 `legacy-spec.json`의 항목이 더해집니다(`legacySpec.js`의 `chatFields`).
**같은 key는 한 항목으로 합쳐집니다.** 새 key를 만들 때 기관 서식과 뜻이 다른데 이름이
겹치지 않도록 주의하세요 (유증 금액을 `bequest_amount`로 둔 이유입니다).

### 2-7. 키는 브라우저와 기부자용 서버에 두지 않는다

모두싸인 **기관 계정** 키는 기관용 서버에만 있습니다. 기부자용은 모두싸인에 문서를 만들거나
서명을 요청하지 않고, 기부자 **개인 계정**으로 자기 문서를 조회만 합니다.

브라우저가 기관용 API를 직접 부르게 만들지 마세요. 지금은 기부자용 서버가 중계하기 때문에
CORS 설정도, 기관 API를 인터넷에 노출할 이유도 없습니다.

### 2-8. 부가 기능이 본 흐름을 막지 않는다

기관 통보처럼 부가적인 호출이 실패해도 기부자의 진행은 계속되어야 합니다.
실패는 레코드에 남기고 다음 조회 때 재시도합니다.

---

## 3. 작업 방식

### 고치기 전에

- 관련 파일을 먼저 읽으세요. 이 저장소의 주석은 **"왜 그렇게 했는지"** 를 적어 둡니다.
  대부분의 이상해 보이는 코드에는 이유가 주석에 있습니다
- 서식·스펙 같은 데이터 구조를 바꿀 때는 **양쪽 웹을 함께** 확인하세요

### 확인

- 서버는 `--reload`(Python) / `--watch`(Node)로 자동 재시작됩니다
- 상태 확인: `curl localhost:8080/api/health`, `curl localhost:4000/api/health`
- 프런트 변경은 `npm --prefix qsight_client/client run build`로 빌드가 깨지지 않는지 보세요
- **테스트했다고 말하려면 실제로 돌려보고 결과를 보여주세요.** 안 해봤으면 안 했다고 쓰세요

### 커밋

한국어로, 제목은 `type(scope): 무엇을`, 본문은 **왜**를 씁니다.

```
fix(W9): 리포트를 '발행'이 아닌 '초안'으로 바로잡고 집계 오류 수정

담당자가 발송된 것으로 오해할 수 있어 문구를 바꿨다. 이메일 채널이
연결되어 있지 않아 실제로는 아무것도 나가지 않는다.
```

- 남의 작업이 섞인 워킹 트리에서 `git add -A` 하지 마세요. 경로를 지정해 담으세요
- `.env`는 절대 커밋하지 않습니다. `.env.example`만 갱신합니다
- 브랜치가 갈라져 있으면 강제 푸시로 덮지 말고 먼저 확인하세요

### 문서

코드를 고쳤으면 관련 README도 같이 고칩니다. 특히 흐름·API·환경변수가 바뀌면
[README.md](README.md), [심사위원_안내.md](심사위원_안내.md), 각 폴더 README를 확인하세요.

---

## 4. 자주 건드리는 곳

| 파일 | 무엇 |
| --- | --- |
| `qsight_client/server/src/routes/chat.js` | 일반 약정 챗봇. LLM 호출은 여기 한 곳뿐 |
| `qsight_client/server/src/routes/legacy.js` | 유산기부 전용 API |
| `qsight_client/server/src/lib/fields.js` | 값 정규화, 지어낸 숫자 차단 |
| `qsight_client/server/src/lib/prompts.js` | 추출용 프롬프트와 tool 스펙 |
| `qsight_client/server/config/legacy-spec.json` | **법률 검토 대상 문안** |
| `qsight_client/client/src/format.js` | 챗봇 문장 조립 |
| `qsight_corp/backend/server/services/agreements.py` | 계약 항목 스키마, 약정 생성 |
| `qsight_corp/backend/server/services/parsing.py` | 서식 PDF 파싱 (Upstage) |

AI를 어디에 어떻게 썼는지는 [AI_활용_증빙.md](AI_활용_증빙.md)에 정리되어 있습니다.
